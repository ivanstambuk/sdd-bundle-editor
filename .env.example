# SDD Bundle Editor - Environment Configuration
# Copy this file to .env and customize values as needed

# =============================================================================
# BUNDLE CONFIGURATION (REQUIRED for development)
# =============================================================================

# Path to the external sample bundle repository
# Clone from: git@github.com:your-org/sdd-sample-bundle.git (or your bundle repo)
# Used by: E2E tests, unit tests, MockAgentBackend, MCP server
SDD_SAMPLE_BUNDLE_PATH=/home/ivan/dev/sdd-sample-bundle

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server port (default: 3000)
PORT=3000

# =============================================================================
# AGENT BACKEND CONFIGURATION
# Set AGENT_BACKEND_TYPE and corresponding options
# =============================================================================

# Agent backend type: 'mock' | 'cli' | 'http'
# Default: 'mock' (for development/testing)
AGENT_BACKEND_TYPE=mock

# --- CLI Backend Options (when AGENT_BACKEND_TYPE=cli) ---
# AGENT_CLI_COMMAND=codex
# AGENT_CLI_ARGS=["exec", "--full-auto"]

# --- HTTP Backend Options (when AGENT_BACKEND_TYPE=http) ---
# AGENT_HTTP_BASE_URL=https://api.openai.com/v1
# AGENT_HTTP_API_KEY=sk-your-api-key-here
# AGENT_HTTP_MODEL=gpt-4o

# --- DeepSeek (alternative HTTP provider) ---
# DEEPSEEK_API_KEY=your-deepseek-api-key

# =============================================================================
# TESTING CONFIGURATION
# =============================================================================

# Enable test mode (bypasses safety checks like branch validation)
# Only set this in CI/test environments
# TEST_MODE=true

# Skip Playwright's managed web servers (use when servers already running)
# PW_SKIP_WEB_SERVER=true
