id: PROF-JWT-OIDC-DISCOVERY
kind: TokenProfile
executionStatus: active
artifactClass: contract
lifecycleStatus: active
title: JWT OIDC Discovery Extension
description: Extension profile for validators that resolve issuer metadata through OIDC discovery before locating the JWKS endpoint.
profileType: OIDC
profileFamily: OIDC
replayProtectionMode: out-of-scope
inheritsProfileIds:
  - PROF-JWT-REMOTE-JWKS
inheritanceMode: additive
inheritanceFieldModes:
  unsupportedFormatIds: inherit
  usesKeyStrategyIds: replace
  requiresHeaderIds: inherit
  optionalHeaderIds: inherit
  requiresClaimIds: extend
  optionalClaimIds: narrow
  allowsAlgorithmIds: inherit
  definesPipelineStepIds: inherit
  implementsReferenceIds: extend
implementsReferenceIds:
  - OIDC-CORE
acceptsPolicyId: STRUCT-runtime-policy
acceptsContextId: STRUCT-validation-context
returnsModelId: STRUCT-validation-result
usesKeyStrategyIds:
  - KEY-oidc-discovery
requiresClaimIds:
  - CLAIM-iss
optionalClaimIds:
  - CLAIM-aud
  - CLAIM-iat
  - CLAIM-jti
  - CLAIM-sub
governedByAdrIds:
  - ADR-profile-driven-validator-composition
  - ADR-oidc-discovery-binds-issuer-to-key-source
  - ADR-key-resolution-fails-closed
  - ADR-profile-governs-claim-requirements
