id: STRUCT-length-encode-input
kind: DataStructure
title: length_encode Input
description: Integer input model for the helper algorithm `length_encode(x)`.
structureKind: input-model
formatType: scalar
executionStatus: active
artifactClass: contract
lifecycleStatus: active
schemaDefinition:
  type: object
  required: [x]
  additionalProperties: false
  properties:
    x:
      type: Integer
      description: Non-negative integer in the profile-supported `0 .. 2^64 - 1` domain.
samplePayload: |
  ```yaml
  x: 255
  ```
