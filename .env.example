# SDD Bundle Editor — Environment Variables
# Copy to .env and adjust for your setup

# ── Ports ──────────────────────────────────────────────────────────────────
# Web dev server port (webpack)
WEB_PORT=5174

# MCP HTTP server port
MCP_HTTP_PORT=3003

# ── Bundle Path ────────────────────────────────────────────────────────────
# Absolute path to the SDD bundle directory to load on startup
# SDD_BUNDLE_PATH=/path/to/your/sdd-bundle

# ── Playwright / E2E ───────────────────────────────────────────────────────
# Set to 1 to run Playwright tests in headed mode (visible browser)
# PLAYWRIGHT_HEADED=0
